# Frontend (Streamlit Cloud) dependencies ONLY.
# app.py talks to the backend over HTTP and imports nothing from src/, so it
# needs just these two. The backend's full stack lives in requirements-backend.txt
# (Render builds from that file via its Build Command).
streamlit==1.54.0
requests==2.32.5
